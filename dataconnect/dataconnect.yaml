specVersion: "v1beta"
serviceId: "innohub"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "innohub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
